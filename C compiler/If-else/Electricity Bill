#include <stdio.h>
int main()
{
int unit;
printf("The total Unit:",unit);
scanf("%d",&unit);

if(unit <= 100)
{
printf("The total amount for the unit is:%d",unit * 1);
}
else if(unit == 101 && unit <= 200)
{
printf("The total amount for the unit is:%d",unit * 2);
}
else if(unit == 201 && unit <= 300)
{
printf("The total amount for the unit is:%d",unit * 3);
}
else if(unit == 301 && unit <= 400)
{
printf("The total amount for the unit is:%d",unit * 4);
}
else if(unit == 401 && unit <= 500)
{
printf("The total amount for the unit is:%d",unit * 5);
}
else if(unit == 501 && unit <= 700)
{
printf("The total amount for the unit is:%d",unit * 6);
}
else if(unit == 701 && unit <= 1000)
{
printf("The total amount for the unit is:%d",unit * 7);
}
else
{
printf("The total amount for the unit is:%d",unit * 10);
}
}
