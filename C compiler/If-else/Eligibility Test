#include <stdio.h>

int main()
{
    int studentage;
    int age = 18;
    
    scanf("%d", &studentage);

    if (studentage >= age)
        printf("Eligible for voting");
    else
        printf("Not Eligible");

    return 0;
}
