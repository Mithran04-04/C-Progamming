#include <stdio.h>
int main()
{
int year;
printf("Give the Year:",year);
scanf("%d",&year);

if(year%4 == 0)
{
if(year%400 == 0)
{
if(year%100 == 0)
{
printf("The given year %d is leap year",year);
}
}
}
else 
printf("The given year %d is not an leap year",year);
}
